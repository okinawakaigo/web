{
  "name": "@okinawa-care/edge",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --ip 127.0.0.1 --port 8787",
    "check": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply recruitment-metrics --local"
  },
  "dependencies": {
    "@okinawa-care/content": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260906.1",
    "@types/node": "24",
    "typescript": "^5.9.3",
    "wrangler": "4.129.0"
  }
}
